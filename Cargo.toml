[package]
name = "neoui"
version = "0.1.0"
edition = "2024"

[profile.release]
debug = true

[dependencies]
minwin = { version = "0.1.0", path = "../minwin" }
# minwin = { git = "https://github.com/zfphex/minwin", version = "0.1.0" }
fontdue = { git = "https://github.com/zfphex/fontdue", default-features = false, features = ["simd"] }
rustc-hash = "2.1.2"
zune-core = { version = "0.5.1", optional = true }
zune-jpeg = { version = "0.5.15", optional = true }
zune-png = { version = "0.5.2", optional = true }
criterion = { version = "0.5", features = ["html_reports"], optional = true  }
mini = { git = "https://github.com/zfphex/mini", version = "0.1.0", features = ["profile"] }

[features]
parallel = ["fontdue/parallel"]
profile = ["mini/profile"]
bench = ["criterion"]
jpeg = ["dep:zune-jpeg", "dep:zune-core"]
png = ["dep:zune-png", "dep:zune-core"]
image = ["jpeg", "png"]

[[example]]
name = "image"
required-features = ["image"]

[[test]]
name = "layout"
harness = false

[[bench]]
name = "font"
harness = false
required-features = ["bench"]

[[bench]]
name = "rect"
harness = false
required-features = ["bench"]

[[bench]]
name = "cache"
harness = false
required-features = ["bench"]

[[bench]]
name = "scroll"
harness = false
required-features = ["bench"]

[[bench]]
name = "raster"
harness = false
required-features = ["bench"]

[[bench]]
name = "image"
harness = false
required-features = ["bench"]

[[bench]]
name = "hash"
harness = false
required-features = ["bench"]

[[bench]]
name = "draw_rect_fill"
harness = false
required-features = ["bench"]
[[bench]]
name = "measure"
harness = false
required-features = ["bench"]
